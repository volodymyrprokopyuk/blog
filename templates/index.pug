extends base

block main
  for post in posts
    article
      h2: a(href=post.url)=post.title
      p=post.date
      ul
        for tag in post.tags
          li=tag
  h4 FP
  for post in fpPosts
    article
      h2: a(href=post.url)=post.title
      p=post.date
      ul
        for tag in post.tags
          li=tag